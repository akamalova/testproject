export class Book {
   constructor(public bookId: string, public title: string, public description: string, public author: string, public isbn: string, public printYear: string, public readAlready: string) {
   }
}
